# Bot do Telegram (assíncrono) + limitador de taxa para evitar erro 429
python-telegram-bot[rate-limiter]>=21.0,<22.0

# Google Planilhas via Service Account
gspread>=6.0,<7.0
google-auth>=2.28,<3.0

# Servidor HTTP usado no modo webhook (hospedagem)
starlette>=0.37,<1.0
uvicorn[standard]>=0.29,<1.0

# Utilidades
python-dotenv>=1.0,<2.0
tzdata>=2024.1
